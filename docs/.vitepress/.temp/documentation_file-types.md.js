import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ファイル形式","description":"Minecraft が対応しているファイル形式です。","frontmatter":{"title":"ファイル形式","description":"Minecraft が対応しているファイル形式です。","mentions":["Ciosciaa","SirLich"]},"headers":[{"level":2,"title":"レベル","slug":"レベル","link":"#レベル","children":[{"level":3,"title":"ワールド","slug":"ワールド","link":"#ワールド","children":[]},{"level":3,"title":"プロジェクト","slug":"プロジェクト","link":"#プロジェクト","children":[]}]},{"level":2,"title":"アセット","slug":"アセット","link":"#アセット","children":[{"level":3,"title":"Packs","slug":"packs","link":"#packs","children":[{"level":4,"title":"Behavior Packs","slug":"behavior-packs","link":"#behavior-packs","children":[]},{"level":4,"title":"Resource Packs","slug":"resource-packs","link":"#resource-packs","children":[]},{"level":4,"title":"Skin Packs","slug":"skin-packs","link":"#skin-packs","children":[]}]},{"level":3,"title":"World Templates","slug":"world-templates","link":"#world-templates","children":[]}]},{"level":2,"title":"Composites","slug":"composites","link":"#composites","children":[{"level":3,"title":"Add-Ons","slug":"add-ons","link":"#add-ons","children":[]},{"level":3,"title":"Editor Add-Ons","slug":"editor-add-ons","link":"#editor-add-ons","children":[]}]}],"relativePath":"documentation/file-types.md","filePath":"documentation/file-types.md"}');
const _sfc_main = { name: "documentation/file-types.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><p><em>Minecraft</em> には、コンテンツを取り込むためのさまざまなファイル形式があります。<em>Minecraft</em> のファイルはすべて、<code class="shiki"><span>mc…</span></code> 拡張子に変更された ZIP アーカイブです。これらのアーカイブは、現在次の 3 種類に分けられます。</p><ul><li><strong>レベル (<code class="shiki"><span>mcworld</span></code> と <code class="shiki"><span>mcproject</span></code>)</strong>: ワールドやプロジェクトのレベルデータと関連リソース</li><li><strong>アセット (<code class="shiki"><span>mcpack</span></code> と <code class="shiki"><span>mctemplate</span></code>)</strong>: ワールド用の見た目や補助アセット</li><li><strong>複合アーカイブ (<code class="shiki"><span>mcaddon</span></code> と <code class="shiki"><span>mceditoraddon</span></code>)</strong>: 1つまでのワールドまたはプロジェクトと、任意数のアセット種別を取り込むために使います</li></ul><p>Minecraft の各ファイル形式は、任意のファイルとして開くことで Minecraft を起動し、コンテンツを取り込めます。パッケージは取り込まれると、自動的に構成ファイルとフォルダーに展開されます。すでに起動していなければ、ほとんどの形式は通常モードで Minecraft を起動しますが、<code class="shiki"><span>mcproject</span></code> と <code class="shiki"><span>mceditoraddon</span></code> は Editor モードで起動します。</p><h2 id="レベル" tabindex="-1">レベル <a class="header-anchor" href="#レベル" aria-label="Permalink to &quot;レベル&quot;">​</a></h2><p>レベルは、通常のワールドや Editor プロジェクトのセーブデータとリソースを表します。モードに関係なく、すべてのレベルは <code class="shiki"><span>com.mojang</span></code> ディレクトリ内の <code class="shiki"><span>minecraftWorlds</span></code> に取り込まれます。</p><p>既存の保存済みレベルと完全に同一のものを取り込むと、重複した保存済みレベルが作成されます。複合アーカイブに複数のレベルが含まれている場合、ネストされた複合アーカイブを含めて、取り込まれるレベルは1つだけです。</p><h3 id="ワールド" tabindex="-1">ワールド <a class="header-anchor" href="#ワールド" aria-label="Permalink to &quot;ワールド&quot;">​</a></h3><p><code class="shiki"><span>mcworld</span></code> 個別のワールドをまとめたアーカイブ</p><p>ワールドアーカイブは、いくつかの方法で作成できます。</p><ul><li>ワールドディレクトリの <em>中身</em> を ZIP 化し、拡張子を <code class="shiki"><span>zip</span></code> から <code class="shiki"><span>mcworld</span></code> に変更する</li><li>ワールドのゲーム設定画面にある &quot;Export World&quot; ボタンを使う</li><li>Editor モードで、File → Export as → Playable world のメニューからワールドを書き出す。ワールドは <code class="shiki"><span>com.mojang</span></code> フォルダー内の <code class="shiki"><span>projectbackups</span></code> ディレクトリに保存されます。</li><li>Editor モードで、<code class="shiki"><span>/project export world</span></code> コマンドを実行する。ワールドは <code class="shiki"><span>com.mojang</span></code> フォルダー内の <code class="shiki"><span>projectbackups</span></code> ディレクトリに保存されます。</li></ul><p><em>Minecraft</em> が Editor モードで起動している状態でワールドパッケージを取り込むと、ワールドはプロジェクトとして取り込まれます。取り込まれたワールドは Editor モード以外では使えなくなるため、プレイ用にワールドとして再書き出しする必要があります。ワールドアーカイブに同梱された Editor 拡張パックは、Editor モード外での取り込みでも保持されます。</p><h3 id="プロジェクト" tabindex="-1">プロジェクト <a class="header-anchor" href="#プロジェクト" aria-label="Permalink to &quot;プロジェクト&quot;">​</a></h3><p><code class="shiki"><span>mcproject</span></code> 個別の Editor プロジェクトをまとめたアーカイブ</p><p>プロジェクトアーカイブは、2つの方法で作成できます。</p><ul><li>プロジェクトディレクトリの <em>中身</em> を ZIP 化し、拡張子を <code class="shiki"><span>zip</span></code> から <code class="shiki"><span>mcproject</span></code> に変更する。</li><li>ワールドのゲーム設定画面にある &quot;Export Project&quot; ボタンを使う</li><li>Editor モードで、<code class="shiki"><span>/project export project</span></code> コマンドを実行する。ワールドは <code class="shiki"><span>com.mojang</span></code> フォルダー内の <code class="shiki"><span>projectbackups</span></code> ディレクトリに保存されます。</li></ul><p><em>Minecraft</em> が起動していなければ、<code class="shiki"><span>mcproject</span></code> ファイルを開くと Editor モードが起動します。<em>Minecraft</em> が起動していても Editor モードでない場合、<code class="shiki"><span>mcproject</span></code> の取り込みは失敗します。</p><h2 id="アセット" tabindex="-1">アセット <a class="header-anchor" href="#アセット" aria-label="Permalink to &quot;アセット&quot;">​</a></h2><p>アセットアーカイブは、レベル以外のさまざまなコンテンツを 1つずつまとめたものです。</p><ul><li>ビヘイビアパック</li><li>リソースパック</li><li>スキンパック</li><li>ワールドテンプレート</li></ul><p>すべてのアセットアーカイブには、内容を説明する manifest が含まれています。同じ種類の既存アセットアーカイブと、manifest の UUID と version が完全に一致すると、取り込みは失敗します。なお、ビヘイビアパックとリソースパックは UUID / version の空間を共有します。ワールド、プロジェクト、テンプレート内に自己完結しているビヘイビアパックとリソースパックは、取り込み時の重複判定には含まれません。</p><p><code class="shiki"><span>mcpack</span></code> と <code class="shiki"><span>mctemplate</span></code> のどちらも、機能的には同じように動作するようです。何がインストールされているのか分かりやすくするため、ビヘイビアパック、リソースパック、スキンパックには <code class="shiki"><span>mcpack</span></code> を、ワールドテンプレートには <code class="shiki"><span>mctemplate</span></code> を使うのが推奨です。複合アーカイブには、任意数のアセットアーカイブを含められます。</p><h3 id="packs" tabindex="-1">Packs <a class="header-anchor" href="#packs" aria-label="Permalink to &quot;Packs&quot;">​</a></h3><p><code class="shiki"><span>mcpack</span></code> Package representing an individual behavior pack, resource pack, skin pack, or world template. It&#39;s recommended only to use <code class="shiki"><span>mctemplate</span></code> for behavior packs, resource packs, or skin packs.</p><p>Packs are only created manually, by zipping the contents of a behavior pack, resource pack, or skin pack directory and renaming the extension from <code class="shiki"><span>zip</span></code> to <code class="shiki"><span>mcpack</span></code>. Behavior and resource packs are installed globally and do not conflict with matching behavior or resource packs installed in worlds, projects, or templates.</p><h4 id="behavior-packs" tabindex="-1">Behavior Packs <a class="header-anchor" href="#behavior-packs" aria-label="Permalink to &quot;Behavior Packs&quot;">​</a></h4><p>Behavior packs are attached to servers to change or extend gameplay. Behavior packs are installed to the <code class="shiki"><span>behavior_packs</span></code> directory in the <code class="shiki"><span>com.mojang</span></code> folder.</p><p>Development behavior packs must be placed in the <code class="shiki"><span>development_behavior_packs</span></code> directory under <code class="shiki"><span>com.mojang</span></code> manually.</p><h4 id="resource-packs" tabindex="-1">Resource Packs <a class="header-anchor" href="#resource-packs" aria-label="Permalink to &quot;Resource Packs&quot;">​</a></h4><p>Resource packs are attached to clients to affect sounds, visuals, etc. Resource packs are installed to the <code class="shiki"><span>resource_packs</span></code> directory in the <code class="shiki"><span>com.mojang</span></code> folder.</p><p>Development resource packs must be placed in the <code class="shiki"><span>development_resource_packs</span></code> directory under <code class="shiki"><span>com.mojang</span></code> manually.</p><h4 id="skin-packs" tabindex="-1">Skin Packs <a class="header-anchor" href="#skin-packs" aria-label="Permalink to &quot;Skin Packs&quot;">​</a></h4><p>Skin packs are client-only packs for custom skins. Skin packs are installed to the <code class="shiki"><span>skin_packs</span></code> directory in the <code class="shiki"><span>com.mojang</span></code> folder.</p><p>Development skin packs must be placed in the <code class="shiki"><span>development_skin_packs</span></code> directory under <code class="shiki"><span>com.mojang</span></code> manually, but this feature appears non-functional.</p><h3 id="world-templates" tabindex="-1">World Templates <a class="header-anchor" href="#world-templates" aria-label="Permalink to &quot;World Templates&quot;">​</a></h3><p><code class="shiki"><span>mctemplate</span></code> Package representing an individual behavior pack, resource pack, skin pack, or world template. It&#39;s recommended only to use <code class="shiki"><span>mctemplate</span></code> for world templates.</p><p>World templates are installed to the <code class="shiki"><span>world_templates</span></code> directory under <code class="shiki"><span>com.mojang</span></code>. World templates can be constructed in a few different ways:</p><ul><li>Zipping the <em>contents</em> of a world directory, adding a world template manifest, and renaming the extension from <code class="shiki"><span>zip</span></code> to <code class="shiki"><span>mctemplate</span></code></li><li>In Editor mode, using the &quot;Export Template&quot; button on the Game settings screen for a world</li><li>In Editor mode, running the <code class="shiki"><span>/project export template</span></code> command. The world will be saved to the <code class="shiki"><span>projectbackups</span></code> directory in the <code class="shiki"><span>com.mojang</span></code> folder.</li></ul><h2 id="composites" tabindex="-1">Composites <a class="header-anchor" href="#composites" aria-label="Permalink to &quot;Composites&quot;">​</a></h2><p>Composite archives are used to import up to <em>one</em> level archive and any number or combination of asset archives in a single import action. In general, contents to a composite must be packaged. Directories can also be given <em>on the top level</em> of a composite archive for importing asset types (behavior packs, resource packs, skin packs, and world templates) without needing to pre-package them. Nested sub-directories for organization may not be used.</p><p>Composite contents are treated as usual. For example, importing a <code class="shiki"><span>mcaddon</span></code> containing a <code class="shiki"><span>mcworld</span></code> while in Editor mode will import the world as a project.</p><p>Composite archives may also contain any number or nesting of other composite archives, even across <em>Minecraft</em> modes. Nested composite archives cannot be used to get around the singular world import restriction.</p><p>Composites can only be constructed manually by zipping archives and asset types.</p><h3 id="add-ons" tabindex="-1">Add-Ons <a class="header-anchor" href="#add-ons" aria-label="Permalink to &quot;Add-Ons&quot;">​</a></h3><p><code class="shiki"><span>mcaddon</span></code> Generic composite content archive</p><p>Importing a <code class="shiki"><span>mcaddon</span></code> package while <em>Minecraft</em> is launched in Editor mode will import any contained world as a project. The imported world will then be inaccessible outside Editor mode and will need to be re-exported as a world for playing. Asset types are imported as usual.</p><h3 id="editor-add-ons" tabindex="-1">Editor Add-Ons <a class="header-anchor" href="#editor-add-ons" aria-label="Permalink to &quot;Editor Add-Ons&quot;">​</a></h3><p><code class="shiki"><span>mceditoraddon</span></code> Composite content archive for Editor mode</p><p>If <em>Minecraft</em> is not open, launching a <code class="shiki"><span>mcproject</span></code> file will open Editor mode. Importing a <code class="shiki"><span>mcproject</span></code> will fail if <em>Minecraft</em> is open but not in Editor mode.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("documentation/file-types.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const fileTypes = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  fileTypes as default
};
